language:
  - java

services:
  - mysql

env:
- DB=mysql

sudo: false

before_script:
  - "ls"
  - "mysql -u root -e 'source Auth/src/main/resources/db.sql'"

script:
  - "cd Auth"
  - "pwd"
  - "mvn jetty:run &"
  - "cd .."
  - "cd Tests"
  - "python FullAuthTest.py"
